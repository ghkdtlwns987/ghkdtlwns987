---
layout: post
title: "순위"
date: 2026-08-07
problem: "49191"
description: >-
  프로그래머스 순위 — 그래프/플로이드로 순위 확정 풀이
image: /assets/images/covers/ranking.png
---

- Platform : 프로그래머스
- 난이도 : 3
- 언어 : Python
- 날짜 : 2026-08-07

## 문제
문제 링크 : https://school.programmers.co.kr/learn/courses/30/lessons/49191

---

## 문제 설명
문제 설명
n명의 권투선수가 권투 대회에 참여했고 각각 1번부터 n번까지 번호를 받았습니다. 권투 경기는 1대1 방식으로 진행이 되고, 만약 A 선수가 B 선수보다 실력이 좋다면 A 선수는 B 선수를 항상 이깁니다. 심판은 주어진 경기 결과를 가지고 선수들의 순위를 매기려 합니다. 하지만 몇몇 경기 결과를 분실하여 정확하게 순위를 매길 수 없습니다.

선수의 수 n, 경기 결과를 담은 2차원 배열 results가 매개변수로 주어질 때 정확하게 순위를 매길 수 있는 선수의 수를 return 하도록 solution 함수를 작성해주세요.

제한사항
선수의 수는 1명 이상 100명 이하입니다.
경기 결과는 1개 이상 4,500개 이하입니다.
results 배열 각 행 [A, B]는 A 선수가 B 선수를 이겼다는 의미입니다.
모든 경기 결과에는 모순이 없습니다.
입출력 예
n	results	return
5	[[4, 3], [4, 2], [3, 2], [1, 2], [2, 5]]	2
입출력 예 설명
2번 선수는 [1, 3, 4] 선수에게 패배했고 5번 선수에게 승리했기 때문에 4위입니다.
5번 선수는 4위인 2번 선수에게 패배했기 때문에 5위입니다.

---

## 문제 풀이
해당 문제는 `win`, `lose` 를 계산하는 문제이다. 처음엔 단순히 카운팅하면 되지 않을까 싶지만, 직접 이긴 것뿐만 아니라 간접적으로 이긴 관계까지 알아야 하기 때문이다.  
예를 들어 아래와 같은 관계가 있다.

```
A > B
B > C
```

로 카운팅하면 A -> 1승, B -> 1승 1패, C -> 1패 로 기록되는데,  실제로는 `A : 1승` `B : 1승` `C : 0승` 가 된다.  

따라서 이 문제는 `승자`, `패자` 를 각각 카운팅 해야 하는 문제이다.   

또한, 가릴 수 있는 승자를 계산하는 방법은 `승 + 패 = (전체 - 1)` 이다. 
왜냐하면, `이긴 수 + 패배한 수` 를 더했을 때, 자신을 제외한 수가 카운팅 되었을때 비로소 정확히 이겼다고 할 수 있기 때문이다.

---

### Solution

```python
from collections import deque
def solution(n, results):
    win_graph = [[] for _ in range(n + 1)]
    lose_graph = [[] for _ in range(n + 1)]

    for a, b in results:
        win_graph[a].append(b)
        lose_graph[b].append(a)
    
    def bfs(idx, graph):
        q = deque()
        q.append(idx)
        visited = [0] * (n + 1)
        
        while q:
            here = q.popleft()
            
            for next in graph[here]:
                if visited[next]:
                    continue
                q.append(next)
                visited[next] = 1
        
        return visited
    cnt = 0
    for i in range(1, n + 1):
        win = bfs(i, win_graph)
        lose = bfs(i, lose_graph)
        
        if sum(win) + sum(lose) == n - 1:
            cnt += 1
            
    return cnt
```
