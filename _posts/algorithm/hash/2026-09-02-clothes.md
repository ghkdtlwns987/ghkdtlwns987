---
layout: post
title: "의상"
date: 2026-09-02
problem: "42578"
description: >-
  프로그래머스 의상 — 종류별 경우의 수 곱셈으로 조합 수 구하기
---

- Platform : 프로그래머스
- 난이도 : 2
- 언어 : Python
- 날짜 : 2026-09-02

## 문제
문제 링크 : https://school.programmers.co.kr/learn/courses/30/lessons/42578

---

## 문제 설명

코니는 매일 다른 옷을 조합하여 입는 것을 좋아합니다.

예를 들어 코니가 가진 옷이 아래와 같고, 오늘 코니가 동그란 안경, 긴 코트, 파란색 티셔츠를 입었다면 다음날은 청바지를 추가로 입거나 동그란 안경 대신 검정 선글라스를 착용하거나 해야 합니다.

| 종류 | 이름 |
| --- | --- |
| 얼굴 | 동그란 안경, 검정 선글라스 |
| 상의 | 파란색 티셔츠 |
| 하의 | 청바지 |
| 겉옷 | 긴 코트 |

코니는 **각 종류별로 최대 1가지 의상**만 착용할 수 있습니다. 같은 종류의 옷 두 벌을 동시에 입을 수는 없습니다.

착용한 의상의 일부가 겹치더라도, 다른 의상이 겹치지 않거나, 혹은 의상을 추가로 더 착용한 경우에는 서로 다른 방법으로 옷을 착용한 것으로 계산합니다.

코니는 **하루에 최소 한 개의 의상**은 입습니다.

코니가 가진 의상들이 담긴 2차원 배열 clothes가 주어질 때, 서로 다른 옷의 조합의 수를 return 하도록 solution 함수를 작성해주세요.

제한사항
- clothes의 각 행은 [의상의 이름, 의상의 종류]로 이루어져 있습니다.
- 코니가 가진 의상의 수는 1개 이상 30개 이하입니다.
- 같은 이름을 가진 의상은 존재하지 않습니다.
- clothes의 모든 원소는 문자열로 이루어져 있습니다.
- 모든 문자열의 길이는 1 이상 20 이하인 자연수이고 알파벳 소문자 또는 `_`로만 이루어져 있습니다.

입출력 예

| clothes | return |
| --- | --- |
| [["yellow_hat", "headgear"], ["blue_sunglasses", "eyewear"], ["green_turban", "headgear"]] | 5 |
| [["crow_mask", "face"], ["blue_sunglasses", "face"], ["smoky_makeup", "face"]] | 3 |

입출력 예 설명

예제 #1

headgear: yellow_hat, green_turban / eyewear: blue_sunglasses

가능한 조합 5가지:

1. yellow_hat
2. blue_sunglasses
3. green_turban
4. yellow_hat + blue_sunglasses
5. green_turban + blue_sunglasses

예제 #2

face: crow_mask, blue_sunglasses, smoky_makeup → 3가지 (한 종류만 있으므로 하나씩 입는 경우만)

---

## 문제 풀이

모든 옷 조합을 하나씩 나열하면 경우의 수가 빠르게 늘어난다. 대신 **종류(kind)별로 나눠서** 세면 된다.

### 핵심 아이디어

1. **종류별로 묶는다** — `headgear`, `eyewear`처럼 의상 종류를 key로 개수를 센다.
2. **각 종류마다 선택지는 (개수 + 1)** — 그 종류에서 아무것도 안 입거나, 가진 n벌 중 하나를 입는다.
3. **종류끼리는 독립** — headgear 선택과 eyewear 선택은 서로 영향을 주지 않으므로 **곱한다**.
4. **아무것도 안 입는 경우 1가지를 뺀다** — 문제 조건상 최소 1벌은 입어야 한다.

```
전체 조합 = Π (각 종류별 (의상 개수 + 1))
정답     = 전체 조합 - 1
```

### 예제 #1로 확인

| 종류 | 의상 수 | 선택지 (안 입음 + n벌) |
| --- | --- | --- |
| headgear | 2 | 3 |
| eyewear | 1 | 2 |

```
(2 + 1) × (1 + 1) = 3 × 2 = 6
6 - 1 = 5  ✓
```

6가지 중 `아무것도 안 입음` 1가지를 빼면 정답 5가 된다.

### 예제 #2

face 한 종류, 의상 3벌:

```
(3 + 1) - 1 = 3  ✓
```

한 종류만 있을 때는 `(n+1)-1 = n`이므로, n벌 중 하나를 고르는 경우와 같다.

---

## defaultdict

종류별 개수를 셸 때 `defaultdict(int)`를 쓰면, **처음 보는 key에 0부터 시작**한다.

```python
from collections import defaultdict

d = defaultdict(int)
d["headgear"] += 1  # KeyError 없이 0 → 1
```

일반 `dict`는 없는 key에 `+= 1`하면 KeyError가 난다. 해시 테이블에 key가 없을 때 기본값 `0`을 돌려주는 것이 `defaultdict(int)`의 역할이다.

---

### Solution

```python
from collections import defaultdict

def solution(clothes):
    clothes_dict = defaultdict(int)

    for name, kind in clothes:
        clothes_dict[kind] += 1

    answer = 1
    for i in clothes_dict.values():
        answer *= (i + 1)

    return answer - 1
```

- `clothes_dict` : 종류(kind) → 해당 종류 의상 개수
- `answer *= (i + 1)` : 종류마다 (안 입음 + i벌 중 1벌) 선택지를 곱함
- `return answer - 1` : 전부 안 입는 1가지 제외

---

## 정리

| 키워드 | 내용 |
| --- | --- |
| 알고리즘 | 해시 (종류별 count) |
| 핵심 | 종류별 `(n+1)` 곱한 뒤 `-1` |
| 조건 | 종류당 최대 1벌, 최소 1벌 착용 |
| defaultdict | 없는 key → 0으로 시작 |

**독립적인 선택지끼리 곱한다**는 것이 이 문제의 본질이다. 조합을 직접 나열하지 않고, 종류별 경우의 수만 세면 O(의상 개수)로 끝난다.

[완주하지 못한 선수]({{ '/poster/2026/09/02/marathon/' | relative_url }})에서 key별 **개수**를 세었다면, 이번에는 key(종류)별 개수를 모아 **곱셈**으로 조합 수를 구한다.
